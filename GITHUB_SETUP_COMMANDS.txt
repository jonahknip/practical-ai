# Step 1: Initialize Git repository
cd /Users/jonahknip/projects/practical-ai-github
git init

# Step 2: Add all files
git add .

# Step 3: Create initial commit
git commit -m "Initial commit: PRACTICAL AI Framework project"

# Step 4: Create GitHub repository (do this on github.com first)
# Go to github.com → New repository → Name: "practical-ai"

# Step 5: Connect to GitHub (replace YOUR-USERNAME)
git remote add origin https://github.com/YOUR-USERNAME/practical-ai.git

# Step 6: Push to GitHub
git branch -M main
git push -u origin main
